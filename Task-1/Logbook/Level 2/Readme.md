In level two i checked the files using ls -la and found a shell in baroque_works_cache called unlock_vault.sh which i exported the clue i found in level 1 and then used the shell command  and got two files in return which i ran diff on and found the next clue.
