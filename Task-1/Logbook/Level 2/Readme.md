n Level 2, I explored the files using ls -la and found a shell script called unlock_vault.sh inside the baroque_works_cache directory. I then exported the clue I obtained from Level 1 as an environment variable and ran the unlock_vault.sh script. The script generated two files, which I compared using the diff command. By identifying the difference between the two files, I found the next clue.

Commands used: ls -la, cd, export and diff
